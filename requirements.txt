# requirements.txt
# Runtime dependencies for the 11nov01 Flask application.
#
# Install with:
#     pip install -r requirements.txt
#
# All versions are EXACT pins per AAP Rule 6 — no ranges, no 'latest'.
# All packages are verified to exist on PyPI for Python 3.12 on Linux x86_64
# and to be compatible with Flask 3.1.3 (the pinned framework version).
#
# Conditional dependencies (Flask-Cors, Flask-SQLAlchemy, PyJWT, etc.) are
# added per AAP Section 0.6.1.2 once the original Node.js source is supplied.

# --- Core web framework (Flask + Werkzeug + Jinja2 stack) ---
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.3
blinker==1.9.0
MarkupSafe==3.0.3

# --- Environment configuration (replaces npm `dotenv`) ---
python-dotenv==1.1.1

# --- Production WSGI server (replaces Node http.Server + pm2) ---
gunicorn==23.0.0

# --- Conditional dependencies (uncomment when Node source provides the npm dep) ---
# (see AAP Section 0.6.1.2 — kept commented out until source is supplied)
