# requirements-dev.txt
# Development and test-only dependencies. Installs runtime deps too via -r.
#
# Install with:
#     pip install -r requirements-dev.txt
#
# All versions are exact pins per AAP Rule 6 (no ranges, no 'latest').
# Verified compatible with Python 3.12 and Flask 3.1.3.

-r requirements.txt

# --- Test runner & Flask integration ---
pytest==8.4.2
pytest-flask==1.3.0
pytest-cov==6.0.0
pytest-mock==3.14.0

# --- Coverage measurement ---
coverage==7.6.4

# --- Linting & formatting ---
ruff==0.7.4
black==25.9.0

# --- HTTP request mocking (replaces Node `nock`) ---
responses==0.25.3
